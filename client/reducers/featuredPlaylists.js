const featuredPlaylists = (state = {}, action) => {

    switch (action.type) {
        case 'GET_PLAYLISTS':
            return state
        default:
            return state
    }

}

export default featuredPlaylists;